import { inject, Injectable } from "@angular/core";
import { Observable, from, map } from "rxjs";
import { <%= className %>, I<%= className %>Repository } from "@boa/<%= fileName %>/domain";
// import { SupabaseService } from "@boa/infra-util";

@Injectable({ providedIn: 'root' })
export class Supabase<%= className %>Repository implements I<%= className %>Repository {
    // private supabase = inject(SupabaseService);
    private readonly tableName = '<%= fileName %>s';

    get<%= className %>s(): Observable<<%= className %>[]> {
        // Placeholder for Supabase implementation
        return from([] as <%= className %>[]);
    }

    get<%= className %>ById(id: string): Observable<<%= className %> | null> {
        return from([null]);
    }

    async save<%= className %>(item: <%= className %>): Promise<void> {
        // Implementation
    }

    async delete<%= className %>(id: string): Promise<void> {
        // Implementation
    }
}
